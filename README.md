# mavenProfiles

1. Project that uses the profiles function of maven to organize which submodules need to be executed with different parts of a lifecycle.

2. Changes the default packaging place for the jar files of the submodules

3. Deploys with wagon-ssh the jar files of the maven project to a remote repository
